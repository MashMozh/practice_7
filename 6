# Запрашиваем у пользователя ввод максимального количества подписчиков N
try:
    N = int(input("Введите максимальное количество подписчиков N (натуральное число): "))

    # Проверяем, что N является натуральным числом
    if N < 1:
        print("Пожалуйста, введите натуральное число больше 0.")
    else:
        subscribers = 1  # Начальное количество подписчиков

        # Выводим количество подписчиков каждый час
        while subscribers <= N:
            print(subscribers)
            subscribers *= 2  # Удваиваем количество подписчиков

except ValueError:
    print("Пожалуйста, введите корректное натуральное число.")
