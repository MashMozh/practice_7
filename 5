# Запрашиваем у пользователя ввод максимального объема N
try:
    N = float(input("Введите максимальный объем N (в куб. см): "))

    # Проверяем, что N положительное
    if N <= 0:
        print("Пожалуйста, введите положительное число для объема.")
    else:
        volumes = []  # Список для хранения объемов кубиков
        edge_length = 1  # Начальная длина ребра кубика

        # Вычисляем объемы кубиков, пока они не превышают N
        while True:
            volume = edge_length ** 3  # Объем кубика
            if volume > N:
                break  # Выходим из цикла, если объем превышает N
            volumes.append(volume)  # Добавляем объем в список
            edge_length += 1  # Увеличиваем длину ребра на 1 см

        # Выводим объемы кубиков через пробел
        print("Объемы кубиков:", ' '.join(map(str, volumes)))

except ValueError:
    print("Пожалуйста, введите корректное число.")
